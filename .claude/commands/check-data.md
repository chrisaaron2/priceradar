Query PostgreSQL for: total rows in raw_listings grouped by source,
latest scraped_at timestamp per source, count of matched products.
Query BigQuery for: row counts in each fact/dim table.
Present as a summary table.
